#pragma once
const float FRICTION = (7.f / 8.f);
const float MIN_SPEED = FRICTION;
const float MIN_STILL_MOD = (3.f / 8.f);

const int BIT_SHIFT_PADDLE_HEIGHT = 6;
const int BIT_SHIFT_PADDLE_WIDTH = 4;
const int BIT_SHIFT_SPEED = 8;
const int BIT_SHIFT_HEIGHT = 4;

const int BIT_SHIFT_START_SPEED = 7;
const int BIT_SHIFT_BALL_SIZE = 8;

